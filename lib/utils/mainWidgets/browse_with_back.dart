import 'package:flutter/material.dart';

class BrowseWithBack extends StatelessWidget {
  const BrowseWithBack({super.key, required this.title, required this.hasLeadingIcon, required this.withBack});
  final String title;
  final bool withBack;
  final bool hasLeadingIcon;
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
